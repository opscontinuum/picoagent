name = "es-doctor"
version = "0.2.0"
entry = "es_doctor:register"
description = "Elasticsearch diagnostics and cluster administration: health, shards, nodes, ILM, snapshots, templates, log digging, and log<->metric<->APM correlation"
requires = ["picoagent>=0.1"]
skills = ["skills"]
